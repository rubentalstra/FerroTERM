// @generated by ferroterm-fhir-codegen from hl7.fhir.r5.core 5.0.0. DO NOT EDIT.
// Change the emitter (tools/ferroterm-fhir-codegen) and regenerate.
//! A timing schedule that specifies an event that may occur multiple times

/// A timing schedule that specifies an event that may occur multiple times
///
/// Specifies an event that may occur multiple times. Timing schedules are used
/// to record when things are planned, expected or requested to occur. The most
/// common usage is in dosage instructions for medications. They are also used
/// when planning care of various kinds, and may be used for reporting the
/// schedule to which past regular activities were carried out.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Timing {
    /// Unique id for inter-element referencing
    ///
    /// Unique id for the element within a resource (for internal references). This
    /// may be any string value that does not contain spaces.
    pub id: Option<std::string::String>,
    /// Additional content defined by implementations
    ///
    /// May be used to represent additional information that is not part of the
    /// basic definition of the element. To make the use of extensions safe and
    /// managable, there is a strict set of governance applied to the definition and
    /// use of extensions. Though any implementer can define an extension, there is
    /// a set of requirements that SHALL be met as part of the definition of the
    /// extension.
    pub extension: Vec<super::extension::Extension>,
    /// Extensions that cannot be ignored even if unrecognized
    ///
    /// May be used to represent additional information that is not part of the
    /// basic definition of the element and that modifies the understanding of the
    /// element in which it is contained and/or the understanding of the containing
    /// element's descendants. Usually modifier elements provide negation or
    /// qualification. To make the use of extensions safe and managable, there is a
    /// strict set of governance applied to the definition and use of extensions.
    /// Though any implementer can define an extension, there is a set of
    /// requirements that SHALL be met as part of the definition of the extension.
    /// Applications processing a resource are required to check for modifier
    /// extensions. Modifier extensions SHALL NOT change the meaning of any elements
    /// on Resource or DomainResource (including cannot change the meaning of
    /// modifierExtension itself).
    pub modifier_extension: Vec<super::extension::Extension>,
    /// When the event occurs
    ///
    /// Identifies specific times when the event occurs.
    pub event: Vec<super::primitives::DateTime>,
    /// When the event is to occur
    ///
    /// A set of rules that describe when the event is scheduled.
    pub repeat: Option<TimingRepeat>,
    /// C | BID | TID | QID | AM | PM | QD | QOD | +
    ///
    /// A code for the timing schedule (or just text in code.text). Some codes such
    /// as BID are ubiquitous, but many institutions define their own additional
    /// codes. If a code is provided, the code is understood to be a complete
    /// statement of whatever is specified in the structured timing data, and either
    /// the code or the data may be used to interpret the Timing, with the exception
    /// that .repeat.bounds still applies over the code (and is not contained in the
    /// code).
    pub code: Option<super::codeable_concept::CodeableConcept>,
}

/// When the event is to occur
///
/// A set of rules that describe when the event is scheduled.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct TimingRepeat {
    /// Unique id for inter-element referencing
    ///
    /// Unique id for the element within a resource (for internal references). This
    /// may be any string value that does not contain spaces.
    pub id: Option<std::string::String>,
    /// Additional content defined by implementations
    ///
    /// May be used to represent additional information that is not part of the
    /// basic definition of the element. To make the use of extensions safe and
    /// managable, there is a strict set of governance applied to the definition and
    /// use of extensions. Though any implementer can define an extension, there is
    /// a set of requirements that SHALL be met as part of the definition of the
    /// extension.
    pub extension: Vec<super::extension::Extension>,
    /// Length/Range of lengths, or (Start and/or end) limits
    ///
    /// Either a duration for the length of the timing schedule, a range of possible
    /// length, or outer bounds for start and/or end limits of the timing schedule.
    pub bounds: Option<TimingRepeatBounds>,
    /// Number of times to repeat
    ///
    /// A total count of the desired number of repetitions across the duration of
    /// the entire timing specification. If countMax is present, this element
    /// indicates the lower bound of the allowed range of count values.
    pub count: Option<super::primitives::PositiveInt>,
    /// Maximum number of times to repeat
    ///
    /// If present, indicates that the count is a range - so to perform the action
    /// between \[count\] and \[countMax\] times.
    pub count_max: Option<super::primitives::PositiveInt>,
    /// How long when it happens
    ///
    /// How long this thing happens for when it happens. If durationMax is present,
    /// this element indicates the lower bound of the allowed range of the duration.
    pub duration: Option<super::primitives::Decimal>,
    /// How long when it happens (Max)
    ///
    /// If present, indicates that the duration is a range - so to perform the
    /// action between \[duration\] and \[durationMax\] time length.
    pub duration_max: Option<super::primitives::Decimal>,
    /// s | min | h | d | wk | mo | a - unit of time (UCUM)
    ///
    /// The units of time for the duration, in UCUM units Normal practice is to use
    /// the 'mo' code as a calendar month when calculating the next occurrence.
    pub duration_unit: Option<super::primitives::Code>,
    /// Indicates the number of repetitions that should occur within a period. I.e.
    /// Event occurs frequency times per period
    ///
    /// The number of times to repeat the action within the specified period. If
    /// frequencyMax is present, this element indicates the lower bound of the
    /// allowed range of the frequency.
    pub frequency: Option<super::primitives::PositiveInt>,
    /// Event occurs up to frequencyMax times per period
    ///
    /// If present, indicates that the frequency is a range - so to repeat between
    /// \[frequency\] and \[frequencyMax\] times within the period or period range.
    pub frequency_max: Option<super::primitives::PositiveInt>,
    /// The duration to which the frequency applies. I.e. Event occurs frequency
    /// times per period
    ///
    /// Indicates the duration of time over which repetitions are to occur; e.g. to
    /// express "3 times per day", 3 would be the frequency and "1 day" would be the
    /// period. If periodMax is present, this element indicates the lower bound of
    /// the allowed range of the period length.
    pub period: Option<super::primitives::Decimal>,
    /// Upper limit of period (3-4 hours)
    ///
    /// If present, indicates that the period is a range from \[period\] to
    /// \[periodMax\], allowing expressing concepts such as "do this once every 3-5
    /// days.
    pub period_max: Option<super::primitives::Decimal>,
    /// s | min | h | d | wk | mo | a - unit of time (UCUM)
    ///
    /// The units of time for the period in UCUM units Normal practice is to use the
    /// 'mo' code as a calendar month when calculating the next occurrence.
    pub period_unit: Option<super::primitives::Code>,
    /// mon | tue | wed | thu | fri | sat | sun
    ///
    /// If one or more days of week is provided, then the action happens only on the
    /// specified day(s).
    pub day_of_week: Vec<super::primitives::Code>,
    /// Time of day for action
    ///
    /// Specified time of day for action to take place.
    pub time_of_day: Vec<super::primitives::Time>,
    /// Code for time period of occurrence
    ///
    /// An approximate time period during the day, potentially linked to an event of
    /// daily living that indicates when the action should occur.
    pub when: Vec<super::primitives::Code>,
    /// Minutes from event (before or after)
    ///
    /// The number of minutes from the event. If the event code does not indicate
    /// whether the minutes is before or after the event, then the offset is assumed
    /// to be after the event.
    pub offset: Option<super::primitives::UnsignedInt>,
}

/// The \`bounds\[x\]\` choice of \`TimingRepeat\`.
///
/// Either a duration for the length of the timing schedule, a range of possible
/// length, or outer bounds for start and/or end limits of the timing schedule.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum TimingRepeatBounds {
    /// The `Duration` form.
    Duration(super::duration::Duration),
    /// The `Range` form.
    Range(super::range::Range),
    /// The `Period` form.
    Period(super::period::Period),
}
