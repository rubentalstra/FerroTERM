// @generated by notio-fhir-codegen from hl7.fhir.r5.core 5.0.0. DO NOT EDIT.
// Change the emitter (tools/notio-fhir-codegen) and regenerate.
//! A series of measurements taken by a device

/// A series of measurements taken by a device
///
/// A series of measurements taken by a device, with upper and lower limits.
/// There may be more than one dimension in the data.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SampledData {
    /// Unique id for inter-element referencing
    ///
    /// Unique id for the element within a resource (for internal references). This
    /// may be any string value that does not contain spaces.
    pub id: Option<std::string::String>,
    /// Additional content defined by implementations
    ///
    /// May be used to represent additional information that is not part of the
    /// basic definition of the element. To make the use of extensions safe and
    /// managable, there is a strict set of governance applied to the definition and
    /// use of extensions. Though any implementer can define an extension, there is
    /// a set of requirements that SHALL be met as part of the definition of the
    /// extension.
    pub extension: Vec<super::extension::Extension>,
    /// Zero value and units
    ///
    /// The base quantity that a measured value of zero represents. In addition,
    /// this provides the units of the entire measurement series.
    pub origin: super::quantity::Quantity,
    /// Number of intervalUnits between samples
    ///
    /// Amount of intervalUnits between samples, e.g. milliseconds for time-based
    /// sampling.
    pub interval: Option<super::primitives::Decimal>,
    /// The measurement unit of the interval between samples
    ///
    /// The measurement unit in which the sample interval is expressed.
    pub interval_unit: super::primitives::Code,
    /// Multiply data by this before adding to origin
    ///
    /// A correction factor that is applied to the sampled data points before they
    /// are added to the origin.
    pub factor: Option<super::primitives::Decimal>,
    /// Lower limit of detection
    ///
    /// The lower limit of detection of the measured points. This is needed if any
    /// of the data points have the value "L" (lower than detection limit).
    pub lower_limit: Option<super::primitives::Decimal>,
    /// Upper limit of detection
    ///
    /// The upper limit of detection of the measured points. This is needed if any
    /// of the data points have the value "U" (higher than detection limit).
    pub upper_limit: Option<super::primitives::Decimal>,
    /// Number of sample points at each time point
    ///
    /// The number of sample points at each time point. If this value is greater
    /// than one, then the dimensions will be interlaced - all the sample points for
    /// a point in time will be recorded at once.
    pub dimensions: super::primitives::PositiveInt,
    /// Defines the codes used in the data
    ///
    /// Reference to ConceptMap that defines the codes used in the data.
    pub code_map: Option<super::primitives::Canonical>,
    /// Offsets, typically in time, at which data values were taken
    ///
    /// A series of data points which are decimal values separated by a single space
    /// (character u20). The units in which the offsets are expressed are found in
    /// intervalUnit. The absolute point at which the measurements begin SHALL be
    /// conveyed outside the scope of this datatype, e.g.
    /// Observation.effectiveDateTime for a timing offset.
    pub offsets: Option<super::primitives::String>,
    /// Decimal values with spaces, or "E" | "U" | "L", or another code
    ///
    /// A series of data points which are decimal values or codes separated by a
    /// single space (character u20). The special codes "E" (error), "L" (below
    /// detection limit) and "U" (above detection limit) are also defined for used
    /// in place of decimal values.
    pub data: Option<super::primitives::String>,
}
