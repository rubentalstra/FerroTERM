---
name: codename-notio
description: "Notio" is a temporary codename, not the official product name
metadata:
  type: project
---

**Notio** (Latin for "concept") is a CODENAME. The official product name is not
set yet; the owner rebranded away from the earlier name "Nomenclator" and wants a
working codename until the real name is chosen. Everything (the GitHub repo
`rubentalstra/notio`, the local directory, the crate prefix `notio-*`, and all
branding/prose) uses `notio` for now.

**How to apply:** when the owner picks the official name, rename the repo, the
local dir, the crate prefixes, and sweep the branding in README/CLAUDE.md/docs.
Until then, keep using `notio` consistently and do not treat it as final.
See [[architecture-decisions]].
