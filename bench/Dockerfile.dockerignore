# The context of bench/Dockerfile is the repository root (the harness builds
# from the checkout), so this file, not the root .dockerignore, guards it: the
# sources go in, the build outputs, the licensed data, and the site stay out.
.git
target
data
artifacts
bench/results
dist
website
node_modules
